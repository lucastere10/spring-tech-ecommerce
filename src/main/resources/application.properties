spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/springtech-api?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456

spring.flyway.locations=classpath:db/migration,classpath:db/testdata

api.security.token.secret = ${JWT_SECRET:senhazinha-secreta}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

spring.web.resources.add-mappings=false

#TOTP
totp.security.secret = ${TOTP_SECRET:segredozin-totp}
totp.code.length=6
totp.time.period=5

spring.jackson.deserialization.fail-on-unknown-properties=true
spring.jackson.deserialization.fail-on-ignored-properties=true

socket-server.host=192.168.1.5
socket-server.port=8878